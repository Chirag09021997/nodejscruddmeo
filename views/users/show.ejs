<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head'); %>

    <body class="container">
        <%- include('../partials/header'); %>
            <main>

                <div class="card">
                    <div class="card-header">
                        <h4>
                            <%= title %>
                                <a href="/users" class="btn btn-danger float-end">Back</a>
                        </h4>
                    </div>
                    <div class="card-body">
                        <div class="form-group mb-2">
                            <label for="name">Name:</label>
                            <input type="text" class="form-control" id="name" value="<%= users.name %>" disabled>
                        </div>
                        <div class="form-group my-2">
                            <label for="email">Email:</label>
                            <input type="text" class="form-control" id="email" value="<%= users.email %>" disabled>
                        </div>
                        <div class="form-group my-2">
                            <label for="password">Status:</label>
                            <input type="text" class="form-control" id="status" value="<%= users.status %>" disabled>
                        </div>
                    </div>
                </div>
            </main>

            <%- include('../partials/footer'); %>
    </body>

</html>